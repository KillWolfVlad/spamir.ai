[project]
name = "spamirai-dataset-telegram-common"
version = "0.0.0"
description = "AI-Powered spam detection service - Telegram Dataset Common"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/spamirai_dataset_telegram_common"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
